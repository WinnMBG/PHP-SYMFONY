{% extends 'base.html.twig' %}
{% block body %}
<html>
    <div class="container" style="margin-top: 7em;">
        <div class="d-flex">
            <div class="col-md-3">
                <img class="img-fluid" src={{value.image}} alt=""/> 
            </div>
            <div class="col-md-6">
                <h1 class="fw-bold fs-2 text-center"> {{value.name}} </h1>
                <p class="fs-4 text-center">
                    {{value.description}}
                </p>
                <h3 class="text-center display-6 fw-bold my-4"> 
                    {{value.price}} €
                </h3>
            </div>
        </div>
    </div>
</html>
{% endblock %}