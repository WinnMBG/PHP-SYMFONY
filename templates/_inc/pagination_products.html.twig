<nav aria-label="Page navigation example">
  <ul class="pagination justify-content-center">
    {% for i in 1..nb %}<li class="page-item"><a class="page-link" href={{url('productsAll', {page: i})}}>{{i}}</a></li>{% endfor %}
  </ul>
</nav>